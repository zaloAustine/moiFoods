<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moifoods">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Firebase"
        android:allowBackup="true"
        android:icon="@mipmap/moi_foods"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/moi_foods"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.NoActionBar">
        <activity android:name=".BussinessModels.PlaceOrder"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".fragments.Track"
            android:label="@string/title_activity_track" />
        <activity
            android:name=".Aunthetication.Signup"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Aunthetication.Login"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Screens.Custom"
            android:label="@string/title_activity_custom"
            android:theme="@style/NoActionBar" />
        <activity android:name=".Screens.Cart" />
        <activity
            android:name=".Screens.ItemView"
            android:label="@string/title_activity_item_view"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".Screens.HotelMenu"
            android:label="@string/title_activity_hotel_menu"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".Screens.MainMenu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/NoActionBar" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>